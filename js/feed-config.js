// ============================================
// BUSINESS FOUNDERS: From the Page
// ============================================
// The reels shown on the homepage. While this list is empty the whole
// section stays out of the page entirely, no empty box and no blank
// tiles, so it is safe to leave as is until there is content.
//
// TO SWITCH IT ON, add one entry per reel:
//
//   {
//     thumb: "images/reels/reel-01.jpg",              // 9:16, about 720x1280
//     url:   "https://www.instagram.com/reel/ABC123/", // the real reel link
//     caption: "Why follower count is the wrong goal", // optional, one short line
//   },
//
// Save the thumbnails in images/reels/. Three or six entries look best:
// the grid runs three across on a desktop and scrolls sideways on a phone.
//
// Posts work here too. A square image in a 9:16 tile gets cropped to the
// middle, so for posts either crop to vertical first or keep the subject
// centred.

const PAGE_REELS = [
  // Nothing yet. Add entries above the closing bracket to switch the
  // section on. Nothing else needs changing anywhere.
];
